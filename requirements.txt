fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-multipart>=0.0.9       # required for UploadFile support

# Document parsing
PyMuPDF>=1.24.0               # PDF text extraction (fitz)
python-docx>=1.1.0            # DOCX support

# Vector store
chromadb>=0.5.0

# Google Gemini — embeddings (gemini-embedding-001) + LLM (gemini-3.6-flash)
# Zero local RAM: all inference runs on Google's servers
google-genai>=0.3.0          # new SDK: provides `from google import genai`
google-generativeai>=0.8.0   # kept for model listing compatibility

# Dev / testing
python-dotenv>=1.0.0
httpx>=0.27.0
pytest>=8.2.0
pytest-asyncio>=0.23.0
