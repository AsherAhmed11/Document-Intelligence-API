fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-multipart>=0.0.9       # required for UploadFile support

# Document parsing — pick what you need
PyMuPDF>=1.24.0               # PDF text extraction (fitz)
python-docx>=1.1.0            # DOCX support
# pdfplumber>=0.11.0          # Alternative PDF parser — compare both

# Vector store & embeddings
chromadb>=0.5.0
openai>=1.35.0
sentence-transformers>=3.0.0

# LangChain — text splitters + semantic chunker
langchain-experimental>=0.0.61
langchain-openai>=0.1.0
langchain-community>=0.2.0      # HuggingFaceEmbeddings used by chunker.py

# Dev / testing
python-dotenv>=1.0.0            # for compare_embeddings script
httpx>=0.27.0                   # for TestClient
pytest>=8.2.0
pytest-asyncio>=0.23.0
